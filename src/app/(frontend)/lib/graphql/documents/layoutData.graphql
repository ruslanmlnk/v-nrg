query GetLayoutData($locale: LocaleInputType) {
  meUser {
    user {
      id
      email
      firstName
      lastName
      phone
      role
      dealerDiscountPercent
    }
  }
  Products(locale: $locale, limit: 100, sort: "createdAt", where: { price: { greater_than: 0 } }) {
    docs {
      ...ProductFrontendFields
    }
  }
  Categories(locale: $locale, limit: 100, sort: "createdAt") {
    docs {
      id
      title
      description
      slug
      image {
        url
      }
    }
  }
}
